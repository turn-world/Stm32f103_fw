1. UART // MCU에서 흔히 쓰는 직렬 통신 방식
2. SOFCallback // 데이터를  송수신할떄 1ms로 호출이 되는 것 따라서 1ms 단위로 callback 함수가 불리게 됨.
3. boot 프로젝트의 .mxproject 파일이 복사한 프로젝트의 파일을 삭제할 수 있어 삭제해야함
4. 부트로더가 먼저 실행한 후 펌웨어를 실행하게 함 부트로더가 있던 시작 위치가 펌웨어의 시작위치로 옮겨짐.
   반드시 수정해야하는 부분이 있음
   ㄴ 인터럽트 백터 -> 링커 스크립트 -> ST Link디버깅시 옵션 변경
5. 